<%= form_for(@testimonial) do |f| %>
  <% if @testimonial.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@testimonial.errors.count, "error") %> prohibited this testimonial from being saved:</h2>

      <ul>
      <% @testimonial.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <% default_value = 'Name' %>

  <div class="field">
    <% f.label :name %><br>
    <%= f.text_field :name, placeholder: 'Name' %>
  </div>
  <div class="field">
    <% f.label :testimonial %>. <br>
    <%= f.text_area :testimonial, size: '100x8', placeholder: 'Write your testimonial here' %>
  </div>
  <div class="field">
    <% f.label :rating %>)<br>
    <%= f.number_field :rating, placeholder: 'Rating (1 - 5)' %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
